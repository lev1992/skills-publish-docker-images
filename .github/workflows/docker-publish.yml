name: docker Publish

on:
  push:
    branches:
      - manin

permissions:
  contents: read
  packages: write


jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Log in to the Container registry
        uses: docker/login-actions@v3
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
      - name: Build and push Docker image
        run: |
          docker build . --tag ghcr.io/lev1992/skills-publish-docker-image/stackoverflown:main
          docker push ghcr.io/lev1992/skills-publish-docker-image/stackoverflown:main